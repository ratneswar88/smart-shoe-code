name: smart_shoe_app
description: A Flutter app to visualize data from ESP32 Smart Shoe
publish_to: 'none' # This is a private app

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  fl_chart: ^1.0.0
  cupertino_icons: ^1.0.8
  equatable: ^2.0.7
  typed_data: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
