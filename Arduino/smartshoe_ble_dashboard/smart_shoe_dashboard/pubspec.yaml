name: smart_shoe_flutter_app
description: A Flutter app for BLE smart shoe dashboard
publish_to: 'none' # remove if you plan to publish
version: 1.0.0+1

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.5
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter