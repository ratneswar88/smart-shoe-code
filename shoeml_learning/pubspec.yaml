name: shoeml_learning
description: Smart Shoe ML app (split) with global BLE connect.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  android_intent_plus: ^5.1.0
  connectivity_plus: ^6.0.3
  app_settings: ^5.1.1
  flutter:
    sdk: flutter
  flutter_reactive_ble:
  shared_preferences: ^2.3.0
  permission_handler:
  fl_chart: ^0.66.2
  path_provider:
  intl: ^0.19.0
  share_plus: ^10.0.0
  geolocator: ^13.0.1
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  cupertino_icons: ^1.0.6
 
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/madeplus_logo.png
